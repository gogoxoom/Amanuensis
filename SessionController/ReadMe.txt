The adventurously named SilverlightApplication12 is the test application that uses the SessionController.  
The Session Controller is a Library.

They are linked by MEF.  The Client (SilverlightApplication2 in this case) refers to the library and that's about it.  
MEF takes care of the rest.

